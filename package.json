{
  "private": true,
  "version": "1.0.0",
  "description": "Central Appium repository",
  "main": "index.js",
  "scripts": {
    "test": "npx lerna run test",
    "build": "npx lerna run build",
    "shrinkwrap": "npx lerna exec -- npm shrinkwrap"
  },
  "author": "Dan Graham",
  "license": "Apache-2.0",
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
